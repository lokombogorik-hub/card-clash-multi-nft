# ===== Telegram Bot =====
BOT_TOKEN=123456:REPLACE_ME
JWT_SECRET=REPLACE_ME_SUPER_SECRET
JWT_ALG=HS256

# ===== Database (Aiven PostgreSQL) =====
# Aiven обычно требует SSL:
# DATABASE_URL=postgresql://USER:PASSWORD@pg-xxxx.aivencloud.com:PORT/DBNAME?sslmode=require
#
# Если Aiven даёт CA cert и требует verify-full:
# DATABASE_URL=postgresql://USER:PASSWORD@pg-xxxx.aivencloud.com:PORT/DBNAME?sslmode=verify-full
# PG_SSLROOTCERT=/etc/ssl/aiven/ca.pem
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
PG_SSLROOTCERT=

# NEAR
NEAR_RPC_URL=https://rpc.mainnet.near.org

# Escrow contract id (optional for now; useful later for verification)
NEAR_ESCROW_CONTRACT_ID=